name: Cypress Tests
on: [push]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Wait for 100 seconds for Deploy
        run: sleep 100
      - name: Cypress run
        uses: cypress-io/github-action@v2
